{
  "name": "@pulseboard/frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo \"[frontend] Phase 2 — React + Recharts dashboard not scaffolded yet\" && exit 0",
    "build": "echo \"[frontend] Phase 2 — no build yet\" && exit 0",
    "lint": "echo \"[frontend] Phase 2 — no lint yet\" && exit 0",
    "test": "echo \"[frontend] Phase 2 — no tests yet\" && exit 0"
  }
}
