# @pulseboard/infra

Placeholder for cloud-agnostic Infrastructure as Code (Part 3 / Phase 3).

Planned: modular, reusable **Terraform** provisioning a container orchestrator
(EKS/GKE/AKS), managed PostgreSQL (RDS/Cloud SQL), managed Redis, and networking —
retargetable to a second cloud by changing a few variables (`CLOUD_PROVIDER=aws|gcp`).

> Not scaffolded yet. This directory exists so the Turborepo workspace layout is
> established from the start.
